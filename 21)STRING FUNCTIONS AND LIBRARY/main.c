//COMMENT OUT THEN CHECK...EACH FUNCTION..ONE BY ONE..
#include <stdio.h>
#include <string.h>
int main()
{
    char s1[] = "Harry";
    char s2[] = "Bob";
    char s3[42];
    // puts(strcat(s1, s2)); //TYPE 1
    // printf("COMBINE s1 AND s2  IS %s\n",strcat(s1,s2)); //TYPE 2
    // printf("THE LENGTH OF s1 IS %d\n",strlen(s1));//TYPE 1
    // printf("THE LENGTH OF s2 IS %d\n",strlen(s2));//TYPE 1
    // printf("THE REVERSED STRING s1 IS: %s\n",strrev(s1));//TYPE 1
    // printf("THE REVERSED STRING s1 IS: %s");//TYPE 2
    // puts(strrev(s1));//TYPE 2 CONTINUE
    // printf("THE REVERSED STRING s1 IS: %s\n",strrev(s2));//TYPE 1
    // printf("THE REVERSED STRING s2 IS: %s");//TYPE 2
    // puts(strrev(s2));//TYPE 2 CONTINUE
    // strcpy(s3,strcat(s1,s2));
    // puts(s3);
    // printf("THE strcmp FOR s1,s2 returned is %d",strcmp(s1,s2));//RETURN 0 IF SAME..RETURN NEGATIVE VALUE IF ASCII OF FIRST LETTER OF s1 IS LESS THEN FIRST LETTER OF s2...RETURN POSITIVE VALUE IF ASCII OF FIRST LETTER OF s1 IS GREATER THEN FIRST LETTER OF s2
    return 0;
}